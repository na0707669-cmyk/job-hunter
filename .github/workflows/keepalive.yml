name: Keep Render Alive

on:
  schedule:
    - cron: "2,16,30,44,58 * * * *"  # ~14분 간격, 명시적 분 지정
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: curl -s -o /dev/null -w "%{http_code}" https://job-hunter-v28m.onrender.com/
